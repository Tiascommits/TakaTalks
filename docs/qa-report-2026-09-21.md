# QA Report — 2026-09-21

## Summary

QA pass gating the merge of `experimental` (`3c229a9`) into `main` (`d2e16d5`). Three
commits: cookie-less GoatCounter analytics, two video-concept docs, and a refresh of
`todo/my-work/for_you.md`.

Only one of the three touches shipped code. The other two are documentation.

## Scope of change

| Commit | Change | Risk |
|---|---|---|
| `b388b6c` | GoatCounter analytics — new `src/lib/analytics/`, new `src/components/Analytics.tsx`, mounted in the root layout | Touches the **root layout**, so it is on every route. Inert unless `NEXT_PUBLIC_GOATCOUNTER_CODE` is set. |
| `4a27db4` | `docs/Bike.md`, `docs/Car.md` — video-concept notes | None; not referenced by the app. |
| `3c229a9` | `todo/my-work/for_you.md`, `todo/agent-work/README.md` | None; documentation. |

## Results

- **Unit (Vitest):** 247 / 247 passed, 23 files. Includes 6 new cases for
  `countEndpoint()` covering the off state, site-code expansion, custom/self-hosted
  hosts, trailing-slash normalisation, and rejection of non-host input.
- **Typecheck (`tsc --noEmit`):** clean.
- **Production build (`next build`):** passes. Static routes stay static — mounting
  `<Analytics />` in the root layout did **not** de-opt prerendering.
- **Lint:** 1 error, 6 warnings — all pre-existing. The error is
  `src/lib/videos/youtube.ts:34` (`no-explicit-any`), which arrived with the YouTube feed
  commit already on `main` and is unrelated to this change. Nothing new introduced.
- **Manual browser verification (Chromium, production build, count beacons intercepted so
  nothing reached a live GoatCounter site):** exactly one pageview counted per navigation
  across initial load, client-side navigation, the back button, and a full reload — no
  double-count when the script finishes loading after the first effect has run. With the
  env var unset, zero references to `gc.zgo.at` in the served HTML: no script fetched, no
  request made.

## Not run this pass

- **E2E (Playwright):** **not re-run.** Deferred to the repo owner by explicit decision —
  `e2e/global-setup.ts` performs a destructive `prisma migrate reset` on
  `TEST_DATABASE_URL`, which the project requires human sign-off for before an agent
  invokes it.
- Last known e2e state: **56 / 56 passing on 2026-09-20** against `main` at `d2e16d5`,
  i.e. the commit this merge is based on, but *before* the analytics change.
- The untested surface is therefore the root-layout mount specifically. The browser
  verification above exercises the same code path on a production build across several
  routes, but it is not the e2e suite.

## Conclusion

`experimental` is merged into `main` on the owner's decision, with e2e pending their own
run. Every non-destructive check is green. If the e2e run surfaces anything, it will
point at the root-layout mount in `b388b6c`, which is revertible on its own without
touching the two documentation commits.
